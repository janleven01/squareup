export type ButtonProps = {
  title: string;
  className: string;
};

export type SectionHeaderProps = {
  title: string;
  description: string;
  imageUrl: string;
  iconUrl?: string;
  startProject?: boolean;
};
